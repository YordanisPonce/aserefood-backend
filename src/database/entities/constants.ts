export enum DiscountOption{
  PERCENTAGE = 1,
  FIXED_AMOUNT = 2,
}

export enum OrderStatus{
  PAYMENT_PENDING = 1,
  PROCESSING_PAYMENT = 2,
  PAYED = 3,
  CANCELLED = 4,
  DELIVERED = 5,
  REFUNDED = 6,
}